[package]
name = "c_test"
version = "0.1.0"
authors = ["Michael Layzell <michael@thelayzells.com>"]
build = "build.rs"

[build-dependencies]
c = { path = "../", features = ["build"] }

[dependencies]
c = { path = "../", features = ["macro"] }
